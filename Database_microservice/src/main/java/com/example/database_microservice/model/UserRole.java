package com.example.database_microservice.model;

public enum UserRole {
    USER,           // Free user - can join groups, complete tasks
    PREMIUM_USER    // Premium user - can create groups, add participants, full features
}
